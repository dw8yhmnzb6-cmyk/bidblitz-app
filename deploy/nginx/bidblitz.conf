# ═══════════════════════════════════════════════
# BidBlitz V2 — Nginx Configuration für bidblitz.ae
# Frontend static + Backend API reverse proxy
# Install: copy to /etc/nginx/sites-available/bidblitz.ae
#   then: ln -s /etc/nginx/sites-available/bidblitz.ae /etc/nginx/sites-enabled/
#   then: nginx -t && systemctl reload nginx
# ═══════════════════════════════════════════════

# Rate limiting zone
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=5r/s;

# Redirect HTTP → HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name bidblitz.ae www.bidblitz.ae;
    return 301 https://bidblitz.ae$request_uri;
}

# Redirect www → non-www
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name www.bidblitz.ae;

    ssl_certificate /etc/letsencrypt/live/bidblitz.ae/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bidblitz.ae/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    return 301 https://bidblitz.ae$request_uri;
}

# Main server block
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name bidblitz.ae;

    # SSL (Let's Encrypt managed via certbot)
    ssl_certificate /etc/letsencrypt/live/bidblitz.ae/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bidblitz.ae/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # Body size for file uploads (KYC docs, listing photos, ad images)
    client_max_body_size 25M;

    # Frontend root
    root /var/www/bidblitz-new/frontend;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/javascript application/javascript application/json image/svg+xml;

    # ── Auth endpoints: stricter rate limit (vor /api/) ──
    location /api/auth/ {
        limit_req zone=auth_limit burst=5 nodelay;

        proxy_pass http://127.0.0.1:8001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ── Stripe webhook (no rate limit) ──
    location /api/webhook/stripe {
        proxy_pass http://127.0.0.1:8001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ── Backend API reverse proxy ──
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;

        proxy_pass http://127.0.0.1:8001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 30s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        proxy_buffering off;
    }

    # ── Static assets caching ──
    location /static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # ── SPA fallback ──
    location / {
        try_files $uri $uri/ /index.html;
    }

    access_log /var/www/bidblitz-new/logs/nginx_access.log;
    error_log /var/www/bidblitz-new/logs/nginx_error.log;
}
