name: One-time VPS Reboot 2

on:
  push:
    branches:
      - work/cache-update-hardening
    paths:
      - .github/workflows/reboot-vps-once2.yml

permissions:
  contents: read

jobs:
  reboot:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Reboot production VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 212.227.20.190
          username: root
          password: ${{ secrets.VPS_PASSWORD }}
          port: 22
          command_timeout: 1m
          script: |
            set -e
            cat /proc/sys/kernel/random/boot_id > /var/tmp/bidblitz-pre-reboot-bootid
            nohup sh -c 'sleep 5; /sbin/reboot' >/var/tmp/bidblitz-reboot.log 2>&1 &
            echo "Reboot scheduled."

      - name: Wait for server reboot
        run: sleep 50

      - name: Verify reboot and services
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 212.227.20.190
          username: root
          password: ${{ secrets.VPS_PASSWORD }}
          port: 22
          command_timeout: 5m
          script: |
            set -e
            OLD_BOOT_ID="$(cat /var/tmp/bidblitz-pre-reboot-bootid)"
            NEW_BOOT_ID="$(cat /proc/sys/kernel/random/boot_id)"
            echo "Old boot: $OLD_BOOT_ID"
            echo "New boot: $NEW_BOOT_ID"
            test "$OLD_BOOT_ID" != "$NEW_BOOT_ID"
            rm -f /var/tmp/bidblitz-pre-reboot-bootid /var/tmp/bidblitz-reboot.log
            systemctl is-active nginx
            nginx -t
            READY=0
            for i in $(seq 1 60); do
              if curl -fsS --max-time 5 http://127.0.0.1:8001/api/system/version >/dev/null 2>&1; then
                READY=1
                break
              fi
              sleep 3
            done
            test "$READY" = 1
            echo "Local API ready after reboot."

      - name: Verify public website
        run: |
          set -euo pipefail
          READY=0
          for i in $(seq 1 50); do
            if curl -fsS --max-time 8 https://bidblitz.ae/ >/dev/null \
              && curl -fsS --max-time 8 https://bidblitz.ae/api/system/version >/dev/null; then
              READY=1
              break
            fi
            sleep 3
          done
          test "$READY" = 1
          echo "BidBlitz is healthy after VPS reboot."
